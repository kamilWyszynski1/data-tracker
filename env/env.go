package env

const (
	// API_TOKEN_PATH stores path to token.json file.
	API_TOKEN_PATH = "API_TOKEN_PATH"

	// CREDENTIALS_FILE_PATH stores path to credentials.json file.
	CREDENTIALS_FILE_PATH = "CREDENTIALS_FILE_PATH"
)
